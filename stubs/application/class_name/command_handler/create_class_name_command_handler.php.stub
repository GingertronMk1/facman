<?php

declare(strict_types=1);

namespace {{ nameSpace }};

use App\Domain\{{ entityName }}\ValueObject\{{ entityName }}Id;

class Create{{ entityName }}CommandHandler {
    public function __construct(
        public {{ entityName }}RepositoryInterface $repo
    )
    {}

    public function handle(Create{{ entityName }}Command $command): {{ entityName }}Id
    {
        $entity = new {{ entityName }}Entity(
            id: $this->repo->generateId();
        );

        return $this->repo->store($entity);
    }
}